# Build output
target/
*.class

# Maven wrapper jar (regenerate with `mvn -N wrapper:wrapper` if you want it)
.mvn/wrapper/maven-wrapper.jar

# Secrets — never commit
.env
*.env
*.key
*.pem
application-local.yml

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# Logs
*.log

# Transfer bundle / reassembly output (not part of the repo)
ALL_FILES/
*_rebuilt/
